{
  "name": "paradox-engineer",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/demand/fetch-demand.js && node scripts/build/score-opportunities.mjs && node scripts/build/build-sitemap.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "validate": "node --check scripts/demand/fetch-demand.js && node --check scripts/build/score-opportunities.mjs && node --check scripts/seo/prune-routes.mjs && npm test && npx astro check"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.57.4",
    "astro": "7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.2"
  },
  "engines": {
    "node": ">=22"
  }
}

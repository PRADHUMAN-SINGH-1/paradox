import { supabase } from '../../lib/supabase.ts';
type Provider='auto'|'gemini'|'groq'|'cerebras'|'huggingface'|'ollama';
type Field=[string,string,'text'|'ta',string];
const W:Record<string,{label:string;title:string;desc:string;fields:Field[]}>= {resume:{label:'RESUME TAILOR',title:'Make the application fit.',desc:'Tailor a real resume to a real role without inventing evidence.',fields:[['Your resume','resume','ta','Paste your resume here...'],['Job description','jd','ta','Paste the complete job description here...'],['Target role','role','text','Software Engineer']]},interview:{label:'INTERVIEW COACH',title:'Train against the actual role.',desc:'Generate a focused interview plan from the supplied role and background.',fields:[['Job description','jd','ta','Paste the job description here...'],['Your background','bg','ta','Projects, experience, skills, education...'],['Interview stage','stage','text','Technical interview']]},study:{label:'STUDY ENGINE',title:'Turn notes into a revision system.',desc:'Convert source material into explanations, flashcards and exam practice.',fields:[['Topic','topic','text','DBMS — normalization'],['Notes / source','notes','ta','Paste notes or source material...'],['Difficulty / exam','difficulty','text','University exam']]},content:{label:'CONTENT STUDIO',title:'Go from idea to publishable draft.',desc:'Turn a concrete brief into platform-ready content with a strong hook.',fields:[['Idea / brief','brief','ta','Describe what you want to publish...'],['Audience','audience','text','Developers and students'],['Format / platform','format','text','LinkedIn post']]}};
const samples:Record<string,Record<string,string>>={resume:{resume:'Software engineering student. Built React + Node.js applications, REST APIs and PostgreSQL-backed projects.',jd:'Software Engineer\nBuild REST APIs, React interfaces, Git and database-backed products.',role:'Software Engineer'},interview:{jd:'Software Engineer\nReact, JavaScript, REST APIs, PostgreSQL, Git.',bg:'Computer Science student. Full-stack projects using React, Node.js, Express, PostgreSQL and MongoDB.',stage:'Technical interview'},study:{topic:'Database Management Systems — normalization',notes:'1NF requires atomic values. 2NF removes partial dependency. 3NF removes transitive dependency.',difficulty:'University exam'},content:{brief:'Launch announcement for a developer platform combining AI workflows and browser utilities.',audience:'Developers and students',format:'LinkedIn post'}};
const P:Record<string,(g:(id:string)=>string)=>string>={resume:g=>`Tailor the resume to the job. Return match analysis, matched keywords, missing keywords, rewritten bullets using only supplied evidence, a tailored summary, and exact edits. Never invent experience, metrics, employers, education or skills.\nRESUME:\n${g('resume')}\nJOB:\n${g('jd')}\nROLE:\n${g('role')}`,interview:g=>`Create an interview preparation pack from the supplied role and background: 8 questions with answer framework, 3 technical follow-ups, 3 behavioural questions, a short introduction and 5 pitfalls. Do not invent experience.\nJOB:\n${g('jd')}\nBACKGROUND:\n${g('bg')}\nSTAGE:\n${g('stage')}`,study:g=>`Create a reliable revision pack from the supplied material: explanation, key concepts, 12 flashcards, 10 exam questions with answers, common mistakes and a one-day plan. Do not fabricate source material.\nTOPIC:\n${g('topic')}\nMATERIAL:\n${g('notes')}\nLEVEL:\n${g('difficulty')}`,content:g=>`Turn this brief into a publishable ${g('format')}. Give 3 hooks, the final draft and a short CTA. Match the audience. Avoid filler and unsupported claims.\nIDEA:\n${g('brief')}\nAUDIENCE:\n${g('audience')}`};
const v=(r:HTMLElement,id:string)=>(r.querySelector<HTMLInputElement|HTMLTextAreaElement>('#'+id)?.value||'').trim();
function init(){const root=document.querySelector<HTMLElement>('.studio');if(!root||root.dataset.controllerReady==='true')return;root.dataset.controllerReady='true';const label=root.querySelector<HTMLElement>('#flowLabel'),title=root.querySelector<HTMLElement>('#flowTitle'),desc=root.querySelector<HTMLElement>('#flowDescription'),status=root.querySelector<HTMLElement>('#status'),result=root.querySelector<HTMLElement>('#result'),empty=root.querySelector<HTMLElement>('#resultEmpty'),error=root.querySelector<HTMLElement>('#error'),provider=root.querySelector<HTMLSelectElement>('#provider'),run=root.querySelector<HTMLButtonElement>('#run'),example=root.querySelector<HTMLButtonElement>('#example'),clear=root.querySelector<HTMLButtonElement>('#clear'),copy=root.querySelector<HTMLButtonElement>('#copy'),download=root.querySelector<HTMLButtonElement>('#download'),metaProvider=root.querySelector<HTMLElement>('.px-result-provider'),metaLatency=root.querySelector<HTMLElement>('.px-result-latency');let current=new URLSearchParams(location.search).get('workflow')||'resume';if(!W[current])current='resume';const render=(flow:string,reset=true)=>{current=flow;const w=W[flow];if(label)label.textContent=w.label;if(title)title.textContent=w.title;if(desc)desc.textContent=w.desc;const f=root.querySelector<HTMLElement>('#fields');if(f)f.innerHTML=w.fields.map(([l,id,t,p])=>t==='ta'?`<label for="${id}"><span>${l}</span><textarea id="${id}" placeholder="${p}"></textarea></label>`:`<label for="${id}"><span>${l}</span><input id="${id}" placeholder="${p}"></label>`).join('');root.querySelectorAll<HTMLButtonElement>('.flow').forEach(b=>b.classList.toggle('active',b.dataset.flow===flow));if(reset){result!.hidden=true;empty!.hidden=false;error!.hidden=true;status!.textContent='READY';metaProvider!.textContent='AI';metaLatency!.textContent='READY';}};root.querySelectorAll<HTMLButtonElement>('.flow').forEach(b=>b.addEventListener('click',()=>{const f=b.dataset.flow||'resume';render(f);const u=new URL(location.href);u.searchParams.set('workflow',f);history.replaceState(null,'',u.pathname+u.search);}));example?.addEventListener('click',()=>{Object.entries(samples[current]).forEach(([id,val])=>{const e=root.querySelector<HTMLInputElement|HTMLTextAreaElement>('#'+id);if(e)e.value=val;});status!.textContent='EXAMPLE LOADED';});clear?.addEventListener('click',()=>render(current));copy?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(result?.textContent||'');status!.textContent='COPIED';}catch{}});download?.addEventListener('click',()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([result?.textContent||''],{type:'text/plain'}));a.download=`paradox-${current}-result.txt`;a.click();});run?.addEventListener('click',async()=>{const w=W[current];if(w.fields.some(x=>!v(root,x[1]))){error!.textContent='Complete every input first.';error!.hidden=false;return;}run.disabled=true;run.textContent='RUNNING…';status!.textContent='ROUTING AI';error!.hidden=true;result!.hidden=false;empty!.hidden=true;result!.textContent='Generating structured output…';const t=performance.now();try{const {data,error:fnError}=await supabase!.functions.invoke('ai-router',{body:{provider:(provider?.value||'auto') as Provider,task:`${current} AI workflow`,prompt:P[current](id=>v(root,id)),system:'You are PARADOX AI Studio. Be accurate, concrete and transparent. Never invent credentials, metrics, sources or achievements.'}});if(fnError)throw new Error(fnError.message||'AI router request failed.');if(!data?.text)throw new Error(data?.error||'No AI output returned.');result!.textContent=data.text;status!.textContent=`DONE · ${String(data.provider||'AI').toUpperCase()}`;metaProvider!.textContent=String(data.provider||'AI').toUpperCase();metaLatency!.textContent=`${Math.round(data.latencyMs||performance.now()-t)}MS`;}catch(e){result!.hidden=true;empty!.hidden=false;error!.textContent=e instanceof Error?e.message:'AI request failed.';error!.hidden=false;status!.textContent='ERROR';}finally{run.disabled=false;run.textContent='RUN AI ↗';}});render(current)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();export {};
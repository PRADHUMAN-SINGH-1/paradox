const startStudio=()=>{const root=document.querySelector<HTMLElement>('#studioPage,.studio');if(!root||root.dataset.studioUiReady==='true')return;root.dataset.studioUiReady='true';document.documentElement.classList.add('studio-runtime');
const tabs=[...root.querySelectorAll<HTMLButtonElement>('[data-section-tab]')];const panels={workflows:root.querySelector<HTMLElement>('#studioWorkflows'),toolkit:root.querySelector<HTMLElement>('#studioToolkit'),radar:root.querySelector<HTMLElement>('#studioRadar')};
const setSection=(section:'workflows'|'toolkit'|'radar',writeUrl=true)=>{tabs.forEach(tab=>{const active=tab.dataset.sectionTab===section;tab.classList.toggle('active',active);tab.setAttribute('aria-selected',String(active));});Object.entries(panels).forEach(([key,panel])=>{if(panel)panel.hidden=key!==section;});if(writeUrl){const u=new URL(location.href);u.searchParams.delete('section');u.searchParams.delete('workflow');u.searchParams.delete('tool');if(section==='toolkit'){u.searchParams.set('section','toolkit');const active=root.querySelector<HTMLElement>('.studio-tool-select.active');if(active?.dataset.toolSelect)u.searchParams.set('tool',active.dataset.toolSelect);}else if(section==='radar')u.searchParams.set('section','radar');else u.searchParams.set('workflow',root.querySelector<HTMLElement>('.flow.active')?.dataset.flow||'resume');history.replaceState(null,'',u.pathname+u.search);}if(section==='radar')void loadRadar(true);};
const chooseTool=(button:HTMLElement)=>{root.querySelectorAll<HTMLElement>('.studio-tool-select').forEach(item=>item.classList.toggle('active',item===button));setSection('toolkit',false);window.dispatchEvent(new CustomEvent('paradox:ai-tool-change',{detail:{mode:button.dataset.toolMode||'',title:button.querySelector('span')?.childNodes[0]?.textContent?.trim()||'Developer Tool',intro:button.querySelector('small')?.textContent?.trim()||''}}));const u=new URL(location.href);u.searchParams.set('section','toolkit');if(button.dataset.toolSelect)u.searchParams.set('tool',button.dataset.toolSelect);history.replaceState(null,'',u.pathname+u.search);};
tabs.forEach(tab=>tab.addEventListener('click',()=>{const section=(tab.dataset.sectionTab||'workflows') as 'workflows'|'toolkit'|'radar';if(section==='toolkit'){const active=root.querySelector<HTMLElement>('.studio-tool-select.active')||root.querySelector<HTMLElement>('.studio-tool-select');if(active)chooseTool(active);else setSection('toolkit');}else setSection(section);}));
root.querySelectorAll<HTMLButtonElement>('.flow').forEach(button=>button.addEventListener('click',()=>{const flow=button.dataset.flow||'resume';root.querySelectorAll<HTMLElement>('.flow').forEach(item=>item.classList.toggle('active',item===button));setSection('workflows',false);const u=new URL(location.href);u.searchParams.set('workflow',flow);u.searchParams.delete('section');u.searchParams.delete('tool');history.replaceState(null,'',u.pathname+u.search);window.dispatchEvent(new CustomEvent('paradox:workflow-change',{detail:{flow}}));}));root.querySelectorAll<HTMLButtonElement>('.studio-tool-select').forEach(button=>button.addEventListener('click',()=>chooseTool(button)));
let radarLoaded=false;async function loadRadar(force=false){if(radarLoaded&&!force)return;const models=root.querySelector<HTMLElement>('#radarModels'),github=root.querySelector<HTMLElement>('#radarGithub'),news=root.querySelector<HTMLElement>('#radarNews'),status=root.querySelector<HTMLElement>('#radarStatus');if(!models||!github||!news||!status)return;radarLoaded=true;status.textContent='FETCHING…';const refresh=root.querySelector<HTMLButtonElement>('#radarRefresh');if(refresh)refresh.disabled=true;const fetchJson=async(url:string)=>{const c=new AbortController();const timer=window.setTimeout(()=>c.abort(),12000);try{const response=await fetch(url,{signal:c.signal,cache:'no-store'});if(!response.ok)throw new Error(String(response.status));return await response.json();}finally{window.clearTimeout(timer);}};const esc=(s:unknown)=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));const results=await Promise.allSettled([fetchJson('https://huggingface.co/api/models?sort=trending&direction=-1&limit=8'),fetchJson('https://api.github.com/search/repositories?q=AI+OR+LLM&sort=stars&order=desc&per_page=8'),fetchJson('https://hn.algolia.com/api/v1/search?query=AI%20LLM&tags=story&hitsPerPage=8')]);const modelsData=results[0].status==='fulfilled'?(results[0].value||[]):[],repos=results[1].status==='fulfilled'?(results[1].value.items||[]):[],stories=results[2].status==='fulfilled'?(results[2].value.hits||[]):[];models.innerHTML=modelsData.length?modelsData.map((item:any,i:number)=>`<a class="studio-radar-card" href="https://huggingface.co/${esc(item.id)}" target="_blank" rel="noreferrer"><strong>${String(i+1).padStart(2,'0')} · ${esc(item.id)}</strong><span>${Number(item.downloads||0).toLocaleString()} downloads · ${Number(item.likes||0).toLocaleString()} likes</span></a>`).join(''):'<p>Model feed unavailable right now.</p>';github.innerHTML=repos.length?repos.map((item:any)=>`<a class="studio-radar-card" href="${esc(item.html_url)}" target="_blank" rel="noreferrer"><strong>${esc(item.full_name||item.name)}</strong><span>★ ${Number(item.stargazers_count||0).toLocaleString()} · ${esc(item.language||'AI project')}</span></a>`).join(''):'<p>GitHub feed unavailable right now.</p>';news.innerHTML=stories.length?stories.map((item:any)=>`<a class="studio-radar-card" href="${esc(item.url||'https://news.ycombinator.com/')}" target="_blank" rel="noreferrer"><strong>${esc(item.title||'AI story')}</strong><span>${Number(item.points||0)} points · ${Number(item.num_comments||0)} comments</span></a>`).join(''):'<p>News feed unavailable right now.</p>';status.textContent=`${[modelsData,repos,stories].filter(x=>x.length).length}/3 LIVE`;if(refresh)refresh.disabled=false;}
root.querySelector<HTMLButtonElement>('#radarRefresh')?.addEventListener('click',()=>void loadRadar(true));
const params=new URLSearchParams(location.search);const requested=params.get('section');if(requested==='radar')setSection('radar',false);else if(requested==='toolkit'){const slug=params.get('tool');const match=slug?root.querySelector<HTMLElement>(`.studio-tool-select[data-tool-select="${CSS.escape(slug)}"]`):root.querySelector<HTMLElement>('.studio-tool-select');if(match)chooseTool(match);else setSection('toolkit',false);}else setSection('workflows',false);};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startStudio,{once:true});else startStudio();
export {};
"use client"; import {useState} from "react"; export default function GeneratorTool({query=""}:{query?:string}){const [value,setValue]=useState("Press generate");return <div className="space-y-4"><button className="rounded-lg bg-[var(--ink)] px-5 py-3 font-semibold text-[var(--canvas)]" onClick={()=>setValue(query ? `${query} — ${Math.floor(Math.random()*10000)}` : String(Math.floor(Math.random()*10000)))}>Generate</button><div className="rounded-xl border border-[var(--line)] p-6 font-mono text-xl">{value}</div></div>}